---
active: true
iteration: 1
max_iterations: 500
completion_promise: "DONE"
initial_completion_promise: "DONE"
started_at: "2026-07-07T05:37:40.633Z"
session_id: "ses_0c4eb4c16ffevprWmsixJketG5"
ultrawork: true
strategy: "continue"
message_count_at_start: 2
---
<mock_detection_protocol>
Before ANY task, check intent. If about to create `.test.*`, `_test.*`, `*Tests.*`, `test_*` files, import mock libraries (`nock`, `sinon`, `jest.mock`, `unittest.mock`), use in-memory databases (`SQLite :memory:`, H2), add `TEST_MODE` / `NODE_ENV=test` behavior, fake HTTP, stub auth, seed fake users, or render components only in isolation → STOP. Fix the REAL system instead. No exceptions. The Iron Rule: if the real system doesn't work, fix the real system; validate only through the same interfaces real users experience.
</mock_detection_protocol>

# Task: Fix all 21 `awesome.video` production audit bugs with real-system evidence

**Priority: URGENT
