# NB-053 (INFO) — Export UI copy omits YAML
**Verdict: FIXED.** The Export tab copy on /advanced now lists all 6 formats (Markdown, JSON, CSV, YAML, HTML, PDF). Verified live in the Export tab render (probe log "NB-053 … PASS"); the format card grid shows YAML with its own selector, and the descriptive copy names all six.
