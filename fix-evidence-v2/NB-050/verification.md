# NB-050 — error-envelope drift + login-failure oracle
Fix: (1) all remaining `{ error: '…' }`-only bodies converted to the canonical `{ message }` envelope (awesome-list/nav/switch-list/github-lists/recommendations/feedback/learning-paths/interactions — 16 sites, guarded sed leaving Run16 `{error: code, message}` dual-key bodies untouched); (2) login 401 fallback string unified to "Invalid email or password" (was "Invalid credentials" when the strategy supplied no info.message).
Live probes: POST /api/switch-list {} → {"message":"Raw URL is required"}; POST /api/recommendations/feedback {} (anon) → {"message":"Unauthorized"}; login nonexistent-email and wrong-password → IDENTICAL {"message":"Invalid email or password"}. Grep proof: 0 remaining message-less error bodies in server/routes.ts. 423 lockout (after 5 failures) remains distinguishable BY DESIGN (deliberate defense w/ Retry-After). VERIFIED.
