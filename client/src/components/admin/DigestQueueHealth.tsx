import { useQuery } from "@tanstack/react-query";
import { Activity, AlertTriangle, CheckCircle2, Clock3, Radio } from "lucide-react";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Button } from "@/components/ui/button";

type Health = { transport: { available: boolean; errorCode?: string }; queue: Record<string, Record<string, number>>; recentFailureCodes: { code: string; count: number }[]; oldestQueuedAt: string | null };
const title = (value: string) => value.replace(/_/g, " ").replace(/\b\w/g, (c) => c.toUpperCase());

export default function DigestQueueHealth() {
  const query = useQuery<Health>({ queryKey: ["/api/admin/digests/health"] });
  const total = query.data ? Object.values(query.data.queue).flatMap(Object.values).reduce((sum, count) => sum + count, 0) : 0;
  return <Card data-testid="card-digest-queue-health"><CardHeader><CardTitle className="flex items-center gap-2"><Activity className="h-5 w-5 text-[var(--accent)]" />Digest delivery health</CardTitle><p className="text-sm text-[color:var(--text-2)]">Aggregate transport and queue signals only. No message contents, user data, or tokens are shown.</p></CardHeader><CardContent>{query.isLoading ? <div className="space-y-3" aria-busy="true"><div className="skeleton h-16 w-full" /><div className="skeleton h-28 w-full" /></div> : query.isError ? <div className="p-4 text-sm" role="alert"><p className="text-destructive">Health data is unavailable.</p><Button variant="outline" className="mt-3 min-h-[44px]" onClick={() => void query.refetch()}>Try again</Button></div> : query.data ? <div className="space-y-6"><div className="grid gap-3 sm:grid-cols-3"><div className="border border-[var(--border)] bg-[var(--surface-2)] p-4"><Radio className={`h-5 w-5 ${query.data.transport.available ? "text-[var(--accent)]" : "text-destructive"}`} /><p className="mt-3 text-xs uppercase tracking-[0.12em] text-[color:var(--text-2)]">Transport</p><p className="mt-1 font-semibold">{query.data.transport.available ? "Available" : "Unavailable"}</p>{query.data.transport.errorCode ? <p className="mt-1 font-mono text-xs text-destructive">{query.data.transport.errorCode}</p> : null}</div><div className="border border-[var(--border)] bg-[var(--surface-2)] p-4"><Clock3 className="h-5 w-5 text-[var(--accent)]" /><p className="mt-3 text-xs uppercase tracking-[0.12em] text-[color:var(--text-2)]">Queue total</p><p className="mt-1 font-display text-2xl">{total}</p></div><div className="border border-[var(--border)] bg-[var(--surface-2)] p-4"><CheckCircle2 className="h-5 w-5 text-[var(--accent)]" /><p className="mt-3 text-xs uppercase tracking-[0.12em] text-[color:var(--text-2)]">Oldest queued</p><p className="mt-1 text-sm font-semibold">{query.data.oldestQueuedAt ? new Date(query.data.oldestQueuedAt).toLocaleString() : "None"}</p></div></div><section><h3 className="mb-3 text-sm font-semibold">Queue by channel and status</h3><div className="grid gap-3 sm:grid-cols-2">{Object.entries(query.data.queue).map(([channel, statuses]) => <div key={channel} className="border border-[var(--border)] p-4"><p className="font-mono text-xs uppercase tracking-[0.12em] text-[var(--accent)]">{title(channel)}</p><dl className="mt-3 grid grid-cols-2 gap-2">{Object.entries(statuses).map(([status, count]) => <div key={status} className="flex items-center justify-between text-sm"><dt className="text-[color:var(--text-2)]">{title(status)}</dt><dd className="font-mono">{count}</dd></div>)}</dl></div>)}</div></section><section><h3 className="mb-3 flex items-center gap-2 text-sm font-semibold"><AlertTriangle className="h-4 w-4 text-[var(--accent)]" />Recent failure codes</h3>{query.data.recentFailureCodes.length ? <ul className="space-y-2">{query.data.recentFailureCodes.map((failure) => <li key={failure.code} className="flex items-center justify-between border-b border-[var(--border)] py-2 text-sm"><span className="font-mono">{failure.code}</span><span className="text-[color:var(--text-2)]">{failure.count}</span></li>)}</ul> : <p className="text-sm text-[color:var(--text-2)]">No recent failure codes recorded.</p>}</section></div> : null}</CardContent></Card>;
}