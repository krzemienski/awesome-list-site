# DISABLED - Use deploy-clean.yml instead
name: DISABLED - Deploy to GitHub Pages

on:
  # Disabled to prevent conflicts
  workflow_dispatch:
      - gh-pages
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}
    
    steps:
    - name: Checkout built assets
      uses: actions/checkout@v4
      with:
        ref: ${{ github.ref_name }}
        
    - name: Verify build assets
      run: |
        if [ ! -f "index.html" ]; then
          echo "❌ No build assets found in gh-pages-build branch"
          exit 1
        fi
        
        echo "✅ Build assets verified"
        ls -la
        
    - name: Setup Pages
      uses: actions/configure-pages@v4
      
    - name: Upload to GitHub Pages
      uses: actions/upload-pages-artifact@v3
      with:
        path: '.'
        
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
      
    - name: Deployment summary
      run: |
        echo "🚀 Deployment completed successfully!"
        echo "📍 Site URL: ${{ steps.deployment.outputs.page_url }}"