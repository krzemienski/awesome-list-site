# Independent review: report-denominators

Raw scoped review. Assertions are subject to the integrator findings in COMPLETION-REVIEW.md; source inspection is not browser proof. Historical counts and initial candidate findings are not current certification.

Exact generator: `tests/parity/report.mjs`. `renderReport()` lines 67-146 correctly derives denominator from `results.summary` and row statuses; line 72 uses `row.denominator`, while row table (106-110) preserves every row/status/reason. `renderStatus()` lines 148-179 is the bug: line 150 restricts to `rows.filter(row => row.eligibility === "pixel")`; then line 154 computes blocked only within those pixel-eligible rows. In this result blocked rows have eligibility `blocked`, so STATUS reports `0 blocked` and omits all 40. It also excludes token-only UNVERIFIED, evidence-only, and aliases from its status sections. Minimal correction: make status derive counts from all `results.rows` (or at least independently count `rows.filter(row => row.status === ...)`), while retaining a separate `pixelRows` only for the PASS/FAIL denominator wording. Report should distinguish: compared pixel rows = statuses PASS+FAIL+INCOMPLETE (184 = 133+51); blocked = status BLOCKED (40); unverified = status UNVERIFIED (100); aliases = status ALIAS (4); evidence-only = status EVIDENCE (4). Do not inflate denominator with blocked/unverified/alias/evidence. `tests/parity/baseline/2026-09-17T11-42-39-272Z-11572/REPORT.md:8-10` is the authoritative correct breakdown and confirms 332 terminal rows; `docs/parity/STATUS.md:5,22-28` is stale/misleading. Eligibility inventory is 47 pixel screens, 25 token-only, 1 artifact-docs, 10 blocked (`REPORT.md:12-20`).

Supported existing-result regeneration path is the runner’s `--report`: `tests/parity/cli.mjs:20-24,49-52,74-80` and `tests/parity/runner.mjs:142-149`. Command: `npm run test:parity -- --report tests/parity/baseline/2026-09-17T11-42-39-272Z-11572/results.json`. It parses only results.json, derives full selection (`runner.mjs:145-147`), and does not capture/run browser. For a full result it writes that run’s REPORT plus shared `docs/parity/REPORT.md`, `tests/parity/REPORT.md`, and (admin identity) `docs/parity/STATUS.md` (`runner.mjs:122-139`); there is no supported docs-only target—so integrator should run it only when browser is done, or accept the documented shared-report writes. It never fabricates candidate proof: all links/statuses/evidence remain those in the immutable existing results. Note `--report` also chmods the run REPORT writable then restores 0444 (`runner.mjs:130-132,148`), so do not invoke during native browser activity.
