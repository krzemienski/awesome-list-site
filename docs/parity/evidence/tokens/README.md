# Evidence — tokens (canonical tokens and shell geometry)

Narrative: `docs/parity/worklog/tokens.md`. Decisions: `docs/parity/assumptions/tokens.md`.

| file | what it shows |
|---|---|
| `token-diff-before.json` | gate `--json` output before the first CSS edit (5 `--text-3` mismatches, no geometry, no utilities yet) |
| `token-diff-after.json` | gate output after the change (regenerated by the round-5 gate): 165 shared values, 0 mismatches, 7 deviations honoured, geometry 9/9, responsive 2/2, 50/50 combos, utilities 26/26, 8 sibling stylesheets scanned (vendor `tailwindcss` included) with their spliced document positions, the cascade layer order and its ambiguities (none), the document verdict (main sheet reached unconditionally; no misplaced, unresolved or duplicate `@import`), plus the 92-row `canaries` table |
| `canonical-tokens.json` | design model extracted from `styles.css` + `design-systems.jsx` + `app.jsx` + `layout.jsx` (root, systems, accents, geometry, responsive values, utilities, layers) |
| `app-tokens-after.json` | runtime model extracted from `design-system.css` + `design-system.ts` registry, including the merged root, `scoped`/`elsewhere` declarations and every sibling stylesheet's custom properties |
| `gate-mutations.md` | the 91 in-gate canaries (run on every invocation; control passes, every mutation fails on the expected rule), the reviews' file-based bypasses re-run (rounds 2–4 through `--app-css`, round 5 against a mirror with the rewritten `index.css`), the self-review spelling sweep, the Tailwind import-substitution measurements, and the Chromium agreement tables (layer canaries; document canaries compiled and raw) |
| `layer-order-browser.json` | `canonical-token-parity-browser-check.mjs` output: each cascade-layer canary's stylesheet rendered in Chromium at 900px with the computed `display`/`outline-offset`/`--bg`; each document canary's overlay served behind a synthetic `client/index.html` — `index.css` compiled by the app's Tailwind compiler (`browser`) and raw (`rawBrowser`); and the live app document's layer order by first appearance |
| `50-combo-table.md`, `50-combo-results.json` | all 50 system × accent combos selected on `/settings/theme`; computed `--accent/--accent-2/--bg/--text` vs design, 50/50 match; live geometry at 1440/800/375 |
| `filmstrip.md`, `filmstrip-home-375.png`, `filmstrip-settings-theme-375.png`, `filmstrip.json` | 375px screencast of a cold navigation: first document frame already in final colours, attribute timeline |
| `utility-fixture.html`, `utility-computed-diffs.json`, `utility-fixture-canonical-vs-app-1440.png` | same fixture markup under canonical `styles.css` vs the app CSS; only the two documented deviations differ |
| `design-system-showcase-1440.jpg` | the app's `/design-system` showcase after the change |
| `visual-before-after.md`, `after-home-1440.jpg`, `after-category-encoding-codecs-1440.jpg`, `after-admin-1440.jpg` | 1440 full-page captures before/after are byte-identical (SHA table); AFTER copies committed |
| `admin-measure.json`, `after-admin-1920.jpg` | `.admin-dashboard` computed `max-width` vs rendered width at 1920/1440 under the live `--content-max-admin` rule, the round-1 rule and the pre-task fallback (rendered width identical); live capture at 1920 |
| `baseline-compare-report.md`, `baseline-compare-report.json` | `npm run baseline:compare` for `/`, `/category/encoding-codecs`, `/settings/theme`; route and API rows identical to the committed pre-task local compare |
| `gates.md` | every gate with command, verdict and the measured baseline for the pre-existing red ones (`lint`, stale e2e specs) |
