{
  "reconstructedFrom": "runner logs read before the 2026-09-12T16:00Z workspace restart; original diagnostics lost",
  "runs": [
    { "runId": "2026-09-12T10-44 (third full run)", "row": "app.shell.palette@768", "throttleWaitSeconds": 569, "failure": "session lapsed before capture (/api/auth/user reported a visitor)" },
    { "runId": "2026-09-12T12-27-10-807Z-26278", "row": "app.shell.palette@768", "throttleWaitSeconds": 569, "failure": "session lapsed before capture (/api/auth/user reported a visitor)", "diagnostics": "visitor header with Sign in; every deferred row unaffected" }
  ],
  "rootCause": "openSide snapshotted identity.storageState() before awaitThrottleBudget slept; frozen-clock context never refreshes the ~60 s Clerk token",
  "fix": "awaitThrottleBudget first, storageState() last (same order as the 429-retry path)"
}
