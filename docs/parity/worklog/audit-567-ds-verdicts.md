# Task 567 — DS verdicts integrator fragment

> **Integrator note:** this owned fragment is incorporated into
> `DS-AUDIT.md` by the Task 567 documentation pass. Neither this fragment nor
> the aggregate report edits the frozen reference.

## Owned verdict

**Aggregate Task 567 verdict: FIX (acceptance NOT COMPLETE).**

**Static design-system verdict: PASS for the current source/static scope.**
The six static gates pass and the current source resolves the previously
concrete App retry, page-heading, and Home interactive-chip findings. The
canonical documents now describe `data-ds="chip"` as owned by both the
`Badge` chip/accent variants and the interactive `ChipButton` primitive; the
implementation owns the hook and the Home call site does not hand-author it.
Approved runtime evidence is bounded: five sampled default themes pass the
canonical primitive sweep, five systems have loaded checked fonts, ten switch
interaction cells and 50/50 theme persistence are historical smoke evidence,
the approved state merge contains 24 PASS cells with provenance, and the
approved Stage 7 evidence is a merged 39+1 result rather than a raw full-file
PASS. Overall Task 567 acceptance remains **NOT COMPLETE** because Lighthouse
still fails all three floors and final pixel parity is incomplete; the state
aggregate JSON is still to be generated by the main agent.

## Five-system × 11-stage matrix

The matrix is for the sampled system default accent. The approved full sweep
covers 40 combinations: five systems, two viewports, and four surfaces.
Thirty-nine full-run rows are valid with zero classified accent violations;
the one invalid Geist 1440px admin-spinner row is replaced by the targeted
`ink-geist` confirmation, which is PASS with zero violations. Stage 8 has zero
long-form ink offenders. Stage 7 remains REVIEW: the merged 39+1 evidence is
not a raw full-file PASS, and automatic classification is not a substitute
for resolving the written accent rule against the frozen page designs.

| System (accent) | 1 files | 2 applied | 3 boot | 4 chrome | 5 scan | 6 primitives | 7 accent | 8 contrast | 9 fonts | 10 skins | 11 switch |
|---|---|---|---|---|---|---|---|---|---|---|---|
| Editorial (Crimson) | PASS | PASS | PASS | PASS | PASS | PASS | REVIEW | PASS | PASS | PASS | PASS† |
| Terminal (Matrix) | PASS | PASS | PASS | PASS | PASS | PASS | REVIEW | PASS | PASS | PASS | PASS† |
| Geist (Cyan) | PASS | PASS | PASS | PASS | PASS | PASS | REVIEW | PASS | PASS | PASS | PASS† |
| Brutalist (Amber) | PASS | PASS | PASS | PASS | PASS | PASS | REVIEW | PASS | PASS | PASS | PASS† |
| Swiss (Orange) | PASS | PASS | PASS | PASS | PASS | PASS | REVIEW | PASS | PASS | PASS | PASS† |

`†` Stage 11 is PASS only for the retained historical smoke run
(`buttonStrays=[]`, `inputStrays=[]`, `h1Strays=[]`,
`canonicalPrimitives=true`, 50/50 persistence, ten interaction cells, and
80/80 visual-smoke rows). The expanded Home/admin/accent changes invalidate
using that run as current final switch or pixel acceptance.

Evidence boundaries:

- Stages 1–4, 6, and 9 are the five `stageAudits` rows in
  `.cache/audit-567-run/audit-567-summary.json`.
- Stage 5 is the retained static worker evidence; app detectors have zero
  hits and standalone retains 98 already-pinned values with no new drift.
- Stage 10 is `80` system selectors and `26` `data-ds` references in the
  current summary.
- Stage 11 state provenance is split across
  `docs/parity/evidence/audit-567-approved/states-initial/`,
  `states-corrected/`, and `states-error/`: respectively 8 PASS/2 BLOCKED/
  2 FAIL, 6 PASS/2 BLOCKED, and 2 PASS. The approved merge of the earlier
  expanded and approved initial evidence, with corrected and final-error
  replacements, contains 24 PASS state cells; the main agent will generate
  the aggregate JSON. E1's 50/50 persistence, ten interaction cells, and
  80/80 visual smoke rows remain historical switch evidence, not a pixel-diff
  or complete current click-through claim.

## Ink values and all captured contrast ratios

These are the exact Stage 8 values from the current summary. `onBg` and
`onSurface` are ratios; no low ratio is silently promoted to body-copy use.

| System | Ink value | on `--bg` | on `--surface` |
|---|---|---:|---:|
| Editorial | `text #f4f3ee` | 18.901 | 18.230 |
| Editorial | `text2 rgba(244, 243, 238, 0.66)` | 8.058 | 7.967 |
| Editorial | `text3 rgba(244, 243, 238, 0.52)` | 5.193 | 5.213 |
| Editorial | `text4 rgba(244, 243, 238, 0.22)` | 1.723 | 1.785 |
| Terminal | `text #e8e8e0` | 17.050 | 16.815 |
| Terminal | `text2 rgba(232,232,224,0.62)` | 6.535 | 6.518 |
| Terminal | `text3 rgba(232,232,224,0.52)` | 4.780 | 4.788 |
| Terminal | `text4 rgba(232,232,224,0.2)` | 1.553 | 1.572 |
| Geist | `text #fafafa` | 20.119 | 18.946 |
| Geist | `text2 rgba(250,250,250,0.62)` | 7.556 | 7.460 |
| Geist | `text3 rgba(250,250,250,0.52)` | 5.465 | 5.497 |
| Geist | `text4 rgba(250,250,250,0.2)` | 1.638 | 1.743 |
| Brutalist | `text #f5f5f0` | 19.202 | 18.486 |
| Brutalist | `text2 rgba(245,245,240,0.7)` | 9.171 | 9.026 |
| Brutalist | `text3 rgba(245,245,240,0.52)` | 5.260 | 5.281 |
| Brutalist | `text4 rgba(245,245,240,0.22)` | 1.733 | 1.799 |
| Swiss | `text #fafaf8` | 20.095 | 19.560 |
| Swiss | `text2 rgba(250,250,248,0.62)` | 7.547 | 7.503 |
| Swiss | `text3 rgba(250,250,248,0.52)` | 5.459 | 5.472 |
| Swiss | `text4 rgba(250,250,248,0.2)` | 1.637 | 1.681 |

Approved Stage 7/8 full-run evidence records 442 visible computed accent users
across the 40 rows. Thirty-nine rows have zero classified violations; the
remaining Geist 1440px admin-overview spinner row is invalidated and replaced
by `docs/parity/evidence/audit-567-approved/ink-geist/results.json`, which
records zero violations and zero long-form ink offenders. This is retained as
merged evidence, not a threshold waiver or raw full-file PASS.

## Static command evidence

All six commands below have exit-0 transcripts in
`docs/parity/evidence/audit-567-approved/static/`; the retained
`.cache/audit-567-static-resume/` copies remain historical:

| Command | Evidence | Result |
|---|---|---|
| `node scripts/validation/palette-drift.mjs` | `palette-drift.log` | PASS; 0 app detector hits beyond baseline |
| `node scripts/validation/accent-drift.mjs` | `accent-drift.log` | PASS; 5 systems, 10 accents, registry/CSS/font parity |
| `node scripts/validation/standalone-palette-drift.mjs` | `standalone-palette-drift.log` | PASS; 3 roots/86 files, no new values; 98 pinned residuals |
| `npm run validate:webfont-fetch` | `webfont.log` | PASS; six stylesheet URLs HTTP 200 and requested faces served |
| `npm run validate:theme-registry-types` | `registry.log` | PASS; baseline plus six rejection mutations |
| `npm run validate:canonical-token-parity` | `canonical-token-parity.log` | PASS; 165 shared values, 0 mismatches/missing |

The separate approved font pre-paint command is also PASS, with seven checks
(six saved IDs plus the unknown-ID fallback):

```text
npm run validate:font-prepaint
```

Its retained exit-0 record is
`docs/parity/evidence/audit-567-approved/audit-567-final-prepaint.log`.

## Current source verification and coordination note

The current checkout was inspected rather than copying the older static
worklog findings:

1. **App retry controls are fixed.** `client/src/App.tsx:259-265` and
   `:281-287` now use the canonical `Button`; the auth retry at `:638-646`
   uses the canonical link variant. The target-floor finding is not current.
2. **Page-heading findings are fixed where the source changed.**
   `client/src/App.tsx:243,276`, `client/src/pages/Bookmarks.tsx:362`,
   `client/src/pages/GuestBookmarks.tsx:104`, and
   `client/src/pages/ContinueLearning.tsx:194` now use `display-h`.
3. **Home interactive chip is fixed in source.**
   `client/src/components/home/HomePresentation.tsx:194-208` uses
   `ChipButton`, which preserves a native button and owns
   `data-ds="chip"` in `client/src/components/ui/chip-button.tsx`. Do not
   call this a hand-authored hook defect. The current canonical documentation
   now names `ChipButton` alongside `Badge` as a hook owner
   (`docs/DESIGN-SYSTEM.md:255-271` and
   `.agents/skills/verify-design-system/SKILL.md:357-398`), so the earlier
   coordination note is closed and current source Stage 6 is PASS.
4. **Intentional page-title font exceptions are not defects.**
   `ResourceDetail.tsx`/`resource.css`, `SubmitResource.tsx`/`submit.css`,
   `AdminDashboard.tsx`/`admin-shell.css`, `About.tsx`/`about.css`, and the
   legal pages/`system-legal.css` match the frozen canonical page source:
   their body-font title rules are intentional. Do not “restore” them to a
   display font or report them as source fixes.
5. **New accessibility fixes are present.** `RouteFallback` in
   `client/src/App.tsx` has `role="status"` with its loading semantics, and
   `client/src/pages/Onboarding.tsx:395-399` gives `Progress`
   `aria-label="Onboarding progress"`.
6. **Standalone residuals:** the 98 existing allowlisted values in
   `artifacts/mockup-sandbox/...` are a separate cleanup task. Do not edit
   the baseline or treat them as new audit regressions.

## Integrator blockers

- Latest approved compiled Lighthouse:
  **0.66 / 0.59 / 0.76** against unchanged floors **0.88 / 0.73 / 0.77**.
  All three routes fail. Machine-readable scores are in
  `docs/parity/evidence/audit-567-approved/lighthouse/scores.json`.
- The approved `perf:mobile` run exits 0. Its retained three-run medians are
  **Home 2426ms** and **category 5093ms** under the pinned mobile profile;
  `docs/parity/evidence/audit-567-approved/audit-567-verified-mobile.json`
  is timing evidence, not a Lighthouse floor substitute. The retained command
  is `npm run perf:mobile`.
- Approved state provenance is
  `states-initial` (**8 PASS, 2 BLOCKED, 2 FAIL**),
  `states-corrected` (**6 PASS, 2 BLOCKED**), and `states-error`
  (**2 PASS**). The accepted merge of earlier expanded and approved initial
  evidence with the corrected/final-error replacements contains **24 PASS
  state cells with provenance**; the main agent still needs to generate the
  aggregate JSON.
- Axe: current summary is **108 PASS rows with zero serious/critical and zero
  incomplete**, while **12 state-only rows are SKIPPED, not PASS**. Production
  category retains three moderate landmark violations at both captured
  widths.
- Final all-inventory pixel parity is **INCOMPLETE**. The selected command
  included all inventory IDs solely to preserve aggregate report ownership. It
  stopped after **33 failures**, with **159 incomplete** and **40 blocked**
  rows, no passing pixel row, and exit 2:
  `tests/parity/baseline/2026-09-15T02-59-58-779Z-8460/REPORT.md`.
  Only the owned Chromium process was terminated; retained teardown facts are
  `localDeleted=true` and `clerkDeleted=true`, and the unrelated identity was
  left untouched.
- Independent About/admin extensions are approved and implemented as
  expected-only projections, but are **not pixel-passing**. The unchanged
  capture thresholds remain pixelmatch `0.1` and `0.5%` maximum difference,
  with no mask/crop. No complete final regression claim is made.

## Remaining work before a final verdict

The **24 PASS state cells** and **39+1 ink/accent cells** are now retained in
`../evidence/audit-567-approved/state-provenance.json` and
`../evidence/audit-567-approved/ink-provenance.json`. These preserve the source
reports, including the original spinner finding; they are not claims of a new
full sweep. Artifact documentation was regenerated and its generator check
passed (18 chapters, 19 files).

1. Resolve the compiled SSR Lighthouse regressions and repeat all three
   routes at **0.88/0.73/0.77**. The successful `perf:mobile` timing run does
   not satisfy those floors.
2. Complete the unchanged all-inventory pixel gate using independent expected
   references for About/admin. The approved extensions are implemented but not
   pixel-passing; application output must not be copied into expected output.

Task 567 remains **NOT COMPLETE**; do not publish a final PASS or call the task
complete.