# BUG-042 fix
date 2026-07-11
root-cause: og-middleware.ts prerender injector did not include /reset-password or /forgot-password in its static form emission branch — only /login and /register. HTML-grounded crawlers / non-JS clients saw an empty SPA shell with zero inputs.
files-changed:
- server/og-middleware.ts (extended the static form branch at line 650)
## before
GET /reset-password -> 200, <input> count=0, <form> count=0 (BUG-042 reproduction)
## after
GET /reset-password -> 200, <input> count=2 (newPassword + confirmPassword), <form> count=1, password-manager autofill shape present
