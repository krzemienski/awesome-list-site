Canonical validation target: http://localhost:5004/
Container: awesome-phase0-dev (Docker)
Probed: 2026-07-11 via curl HTTP=200
NODE_ENV inside container: production (overridden at runtime, so production-gated headers are active)
DB host: awesome-phase0-db:5432 (docker-compose internal network)
DB confirmed via psql: resources table has 1935 rows.

DO NOT use these stale containers:
- awesome-list-app     on :5001  (22h uptime, stale local build)
- awesome-phase0-app   on :5002  (9h uptime, but a separate stale build)
- awesome-phase0-builder        (still building, non-serving)

All bug probes MUST hit http://localhost:5004/ unless a bug is
specifically host-route or path-specific (then call out the path).
CSP/header bugs can be probed here because the container serves prod build.
