# Full Functional Audit r6 — VERDICT

**Date:** July 10, 2026 · **System:** dev workspace (real Express + PostgreSQL + Vite, no mocks, no test files in repo) · **Evidence:** `evidence/audit-r6/`

## VERDICT: ✅ PASS — 100% of validated items pass; zero functional bugs; one security-hygiene finding (fixed inline, see Observations)

| Sweep | Scope | Result | Evidence |
|---|---|---|---|
| Drift (since r5) | Task #122 migration-drift checker: validation workflow green against a scratch DB (same drizzle migrator code path as prod boot). Task #132 boot-migrator rewrite: `scripts/verify-boot-migration-safety.ts` exit 0 — Scenario A (partial schema completed idempotently) + Scenario B (non-idempotent migration fails LOUDLY, nothing half-applied). Dev boot intentionally skips `runMigrations` (`NODE_ENV=production` gate) so no journal table in dev = expected, not drift. | ALL PASS | `migration-drift-validation.log`, `boot-migration-journal.txt` |
| A — Anonymous API | 45 checks: public reads, search honesty (`total`=200 for ffmpeg exactly == approved-only title+description ILIKE DB count), negative/short-q search edges, journeys, recommendations init, learning-path hardening (limit clamps ±, bogus enum/category params 200-normalized), `/api/health/ai` cheap + deep (real Claude round-trip `connectionOk:true`), all 3 suggested paths `generationType:"ai"`, SEO surface (sitemap/robots/og-image svg+png), public API key endpoints, 15× 401 gates | 45/45 PASS (first run, zero harness errors) | `sweepA-anon-api.txt` |
| B — Authed user flow | 31 checks: register→login→auth/user reflects email→favorites cycle→bookmarks cycle→interaction→API keys (create UUID id, list, Bearer `/api/public/me` 200, delete, Bearer revoked 401)→user progress/submissions/journeys→journey start + progress GET/PUT→submit resource (status forced `pending`)→suggest edit on real 185811→change-password→logout→session cleared→gate after logout→relogin new pw→old pw rejected | 31/31 effective PASS¹ | `sweepB-authed-user.txt` |
| C — Admin flow | 38 checks: non-admin 403 gate, promote (real DB write)+fresh login, 18 admin GETs (incl. researcher/enrichment/github-sync/link-health), markdown export POST, QA resource approve (status verified in DB)→edit create/approve (change APPLIED to DB)→reject r6 QA edit on real 185811 (≥10-char reason; resource md5-verified untouched; edit status `rejected`)→delete→404, category create(slug)/rename/delete (row gone), user2 promote/demote via `PUT /api/admin/users/:id/role` (roles verified in DB) | 38/38 PASS (first run, zero harness errors) | `sweepC-admin.txt` |
| D — Browser UI, anonymous (Playwright, pinned Chromium 1208) | 13 SPA-nav public routes + 8 gotos (lazy `/advanced` bodyLen 14,196; 4 auth pages; `/profile`+`/bookmarks` bounce home; `/admin` AdminGuard in-place denial panel + sign-in link proven on full body text) + 10 redirects (incl. `/?q=` → `/search?q=` and 3-segment category path) + 404 page + search card-click→`/resource/187905` (cards navigate via onClick→setLocation by design, no `<a>`) + sidebar expand→subcategory nav + ⌘K palette (16 results for "av1") + mobile 390px zero horizontal overflow on `/`, category, resource detail | ALL effective PASS² | `sweepD-ui-spa.txt`, `sweepD-ui-goto.txt`, `sweepD-ui-redirects.txt`, `sweepD-ui-interactions.txt` |
| E — Browser UI, authenticated | Real form login (email+password fill, submit)→`/api/auth/user` reflects QA email; `/profile` renders (bodyLen 8,338); `/bookmarks` renders ("My Bookmarks"); `/admin` dashboard renders real data as admin (denial absent, screenshot); logout→`/admin` guard restored (denial panel back, `user:null`) | 6/6 PASS | `sweepE-ui-authed.txt`, `sweepE-admin-dashboard.png` |
| Teardown | Net-zero removal of 2 QA users (memory-guided order: resource_edits by submitted_by/handled_by → audit-log by performed_by → NULL approved_by/submitted_by → name-pattern residue → users cascade → 34 session rows) | 0 residue on every probe; approved=1,838, total=1,994, users=6, categories=9, journeys=5 — **exactly == baseline**, all 9 per-category counts unchanged (diff-verified) | `teardown.txt`, `reconcile.json`, `baseline.json` |

¹ 3 initial FAILs were ONE harness bug: `apiKey.id` is a UUID, the harness grepped `[0-9]+` → empty id → DELETE hit a malformed path (404) → key never revoked (Bearer stayed 200). Full lifecycle re-proven with proper UUID extraction: create→list→Bearer 200→delete 200→Bearer 401. Orphan key from the first attempt deleted (200). Soft-revoked key rows remain by design and were removed by the user-cascade teardown.
² SPA-batch artifacts, all triaged in-file with retests: 1,200 ms pushState wait raced lazy chunks (`/about`, `/journey/6`, `/sub-subcategory` re-proven via goto); `/search` h1 is "Search" with a "200 results for “ffmpeg”" header and 200 rendered ResourceCards — harness h1 expectation was stale; first `/admin` check sliced body text to 3,000 chars and missed the denial panel that sits below the sidebar text — full-body retest PASS. One transient `net::ERR_ABORTED` goto mid-batch; retry logic + rerun clean. Zero app bugs.

## Drift since r5 — all covered and passing
- **#122 migration-drift checker**: `migration-drift` validation workflow green (scratch-DB migrator run, meta-journal parity).
- **#132 boot-migrator rewrite**: both safety scenarios proven on scratch DBs via the repo's own proof script (idempotent completion; loud failure with nothing half-applied).
- Two production publishes since r5: dev-side surface re-validated end-to-end; prod smoke was validated at publish time (out of scope for this dev audit).

## Skipped by design (destructive/expensive) — gates verified instead
Real AI enrichment/researcher runs, GitHub import/export POSTs (mutate the real repo), bulk delete/approve on real data, seed-database — admin auth gates confirmed (401 anon in Sweep A; endpoints reachable as admin in Sweep C GETs).

## Observations (architect review)
- **Password-hash logging (fixed inline):** `/api/auth/user` debug logging dumped the full DB user row — including the bcrypt password hash — into server logs on every authed request. Redacted to `{id,email,role}` (`server/routes.ts`), proven on the real system post-restart: fresh authed request logs the redacted shape, 0 `$2b$` occurrences in the new log (proof appended to `teardown.txt`); temp verification user torn down, net-zero re-verified, `tsc` clean.
- INVENTORY.md initially misstated the boot-migration evidence ("journal table populated" — it is absent in dev by design); corrected to match `boot-migration-journal.txt`.

## Iron Rule compliance
All validation ran against the live dev system: real HTTP, real sessions/cookies, real DB writes (QA-prefixed, fully torn down, net-zero verified against pre-audit baseline incl. per-category diff parity), real Chromium with real form fills/clicks/keyboard. No mocks, no stubs, no test files added to the repo (all harness scripts in `/tmp`).
