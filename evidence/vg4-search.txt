VG-4 — SEARCH (JSON API + UI + /?q= routing)

NOTE ON DEPLOYMENT: the new GET /api/search endpoint, the /?q= 301, and the /search
page changes are in the workspace and validated on the running DEV server. They reach
production on the next publish. Live prod GET /api/search?q=ffmpeg currently returns
HTTP 404 {"message":"Not found"} — expected pre-deploy, proves the endpoint is new.

--- DEV (http://localhost:5000), real server, real browser ---

JSON API (new endpoint):
  GET /api/search?q=ffmpeg -> total 61, non-empty results array, first result
    "chn-lee-yumi/distributed_ffmpeg_transcoding_cluster ..." (FFmpeg-related),
    FFmpeg present in first 5 results = true.  NOT {"message":"Not found"}.
  Honest total (post code-review fix — returns the repo's true match count, not the
  deduped page size): q=codec -> total 111 (results 100, capped by limit),
    q=HLS -> total 160 (results 100), q=ffmpeg -> total 61 (results 61). These match
    the /search UI counts and the /api/resources?search= totals exactly.
  Robustness: limit is clamped to [1,200]; ?limit=-5 -> HTTP 200 (was a 500 from a
    negative SQL LIMIT); q shorter than 2 chars -> {total:0,results:[]}.

UI page (/search, real browser — evidence/vg4-search-<q>.png, READ + verified):
  /search?q=ffmpeg -> "Search" page, "61 results for “ffmpeg”", grid of real
    FFmpeg resources (chn-lee, CUDA GPU Accelerated, mrpdaemon/mmf, sambios/
    ffmpeg_transcoder, fluent-FFmpeg, ffmpeg tutorial, pyke369/sffmpeg, ...).
    Renders the SEARCH page, NOT the "Browse 1838" homepage.
  /search?q=codec -> "111 results for “codec”"   (distinct)
  /search?q=HLS   -> "160 results for “HLS”"      (distinct)

Routing:
  /?q=ffmpeg  ->  finalUrl http://localhost:5000/search?q=ffmpeg
    (client redirect on dev; og-middleware issues a server 301 in prod after deploy).

VERDICT: PASS on dev (real system). Ships to prod on publish.
