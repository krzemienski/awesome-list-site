# Run24D — Responsive, Print & Accessibility Sweep (Task #182)

All acceptance gates re-run live on dev (Playwright, real browser, no mocks).
Harnesses: `print-audit.mjs` (29/29 PASS, `print-audit.json`) and
`responsive-audit.mjs` (17/17 PASS, `responsive-audit.json`), plus one-off
probes logged below. tsc clean; migration-drift clean; no screen regression
(`.print-only` verified 0 visible on screen; home/resource/admin spot-checked
at 1440/1024/375 during sweeps).

| Finding | Disposition | Fix | Evidence |
|---|---|---|---|
| R4-039 (MED) — sort selects still print | fixed (code) | Print un-clamp rule `[class*="line-clamp"]` was resurrecting shadcn SelectTrigger buttons (their class string contains `[&>span]:line-clamp-1`, and the attribute selector out-specifies the `button` hide). Scoped with `:not(button):not([role="combobox"])`. Count badges (`badge-count`) also `no-print`. | `print-audit.json` — `buttons-hidden` PASS on /, /advanced, /search, category, resource, profile; PDFs `home.pdf`, `advanced.pdf`, `category.pdf`, `search.pdf`, `resource.pdf` |
| R4-070 (LOW) — admin prints blank page | fixed (code, CSS-only; AdminDashboard TSX untouched) | Two causes: (1) shadcn sidebar's outer shell (h-svh gap div + fixed 1px-border rail) stayed in flow after `[data-sidebar]` hide — spacer punted content to page 2, rail printed a rule down every blank sheet — now hidden via `.group.peer:has([data-sidebar="sidebar"])` + h-svh/min-h-svh neutralized; (2) `break-inside: avoid` on broad `[class*="rounded-lg"]` wrappers punted taller-than-page cards whole — avoid list rescoped to li/tr/resource cards. Scroll containers expand (`overflow: visible`, `height:auto`). | `admin.pdf` now 1 page, ink-frac 0.0353 (was 3 pages: 0.0000 / 0.0245 / 0.0000); all pages of home/category/resource PDFs ink > 0.028 |
| R5-027 (MED) — print amputates inline buttons / dangling labels / blank swatches | fixed (code) | `button.print-keep-text` exception keeps inline-in-copy button text ("Please **log in** to start…" stays grammatical, JourneyDetail); "Was this helpful?" vote blocks and "Configure Your Preferences" card `no-print` as whole units (RecommendationCard, ai-recommendations-panel); ThemeSettings all 4 picker/preview sections `no-print` (swatches can't print blank if not printed). | `print-audit.json` — `journey-anon:login-inline-visible` PASS (1 keep-text btn visible + sentence intact), `recommendations:helpful-hidden` PASS, `theme-settings:pickers-hidden` PASS (4 sections, 0 visible); PDFs `journey-anon.pdf`, `recommendations.pdf`, `theme-settings.pdf` |
| R5-053 (LOW) — card grids print dead buttons, no URLs | fixed (code) | ResourceCard: Open Link row, View Details anchor, fav/bookmark row all `no-print` (they're anchors, so the blanket button hide never applied); new `.print-only` URL line under each card title (screen-hidden, print `display:block` + `break-all`). Sort selects + count badges hidden (see R4-039). | `print-audit.json` — category/search: `openlink-hidden` 24→0 visible, `viewdetails-hidden` PASS, `url-printed` 24 visible URLs, `title-prints` 24/24; `category.pdf` |
| R5-026 (MED) — profile header overlap 700–900px | fixed (code) | Profile header: `sm:flex-wrap lg:flex-nowrap` on the row + `sm:basis-full lg:basis-auto` on the actions cluster gives Settings/Logout their own row in the 640–1024 band; email wrapped in `min-w-0` + `break-all`. | `responsive-audit.json` — zero rect overlap and name visible at 640/700/768/812/860/900/1024/1280/1440, hOverflow 0 everywhere; `profile-700.png`, `profile-812.png`, `profile-900.png` |
| R5-054 (LOW) — wrapped tab lists render stadium blob | fixed (code) | `ui/tabs.tsx` TabsList: `rounded-lg xl:rounded-full` — card radius (12px) wherever lists can wrap, pill preserved from 1280 up. | `responsive-audit.json` — radius 12px at 375/500 (profile, wrapped) and 768 (admin, wrapped, 5 rows), rounded-full at 1440 single row; `tabs-profile-375.png`, `tabs-admin-768.png`, `tabs-profile-1440.png` |
| R5-056 (LOW) — forced-colors buttons lose affordance | fixed (code) | `@media (forced-colors: active) { button, [role="button"] { border: 1px solid ButtonText } }` in index.css. | `responsive-audit.json` — forced-colors emulation: 30/30 home buttons bordered; probe on /submit + /login: 38/38 each, consent-banner buttons all bordered; `forced-colors-home.png` |
| R5-057 (LOW) — breadcrumb 0×0 at ≤375, no title attrs, role="menu" | fixed (code) | New mobile current-page crumb (`breadcrumb-mobile-current`, `aria-current="page"`, truncating, title attr) shown below md — deep pages now have a location cue at 375/320; `title` attrs added to every BreadcrumbLink/BreadcrumbPage. Ellipsis menu: verified live it opens a Radix menu with `role="menu"` + 2 `role="menuitem"` links at 1024 (report's missing-role claim stale); at ≥1280 the ellipsis is intentionally hidden because the full trail renders (title attrs disambiguate truncation there). | `responsive-audit.json` — mobile crumb 142×20px @375, 121×20 @320, hOverflow 0; titles 4/4 crumbs @1440, 0 static role="menu" misuse; live probe: dropdown `{hasRoleMenu:true, items:2}`; `breadcrumb-375.png`, `breadcrumb-menu-1024.png` |

## Regression checks
- tsc --noEmit: clean.
- migration-drift workflow: clean (8 migrations, no drift).
- Full print audit re-run green AFTER all CSS changes (29/29).
- Screen mode: `.print-only` 0 visible; chrome checks (header/sidebar/buttons) green on all 9 audited routes.
- No server code touched; no files owned by Run24B/C/F touched (AdminDashboard fix is print-CSS only in index.css).
